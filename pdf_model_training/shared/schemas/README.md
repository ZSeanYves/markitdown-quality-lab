# Shared Schemas

This directory is for small, versioned schema documents shared across PDF
model training tasks.

Allowed content:

* schema descriptions
* small JSON schema files
* table schemas for TSV manifests
* compatibility notes

Not allowed:

* raw dataset payloads
* model checkpoints
* generated feature matrices
* prediction dumps
