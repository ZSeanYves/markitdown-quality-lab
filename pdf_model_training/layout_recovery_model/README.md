# Moved: layout_recovery

The canonical parser-facing model directory is now:

```text
pdf_model_training/layout_recovery/
```

The old `layout_recovery_model` name is kept only as a compatibility pointer.
Do not add new manifests, labels, local-only files, or training scripts here.
