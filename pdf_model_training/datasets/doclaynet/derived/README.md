# DocLayNet Derived Subsets

This directory tracks small, reviewable summaries for local-only derived
DocLayNet subsets.

Large adapted rows, extracted JSON, feature matrices, predictions, and model
artifacts remain under model-owned `local_only/` directories.

Current derived subset names:

* `pilot500_v1`
* `pilot1000_v1`
* `pilot3000_v1`
