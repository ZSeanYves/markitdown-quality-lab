# Layout Recovery Scripts

This directory is reserved for parser-facing dataset, feature, train, eval, and
export scripts.

No baseline model has been trained in this scaffold step.

Expected future script groups:

* build DocLayNet region rows
* export parser-side geometry features
* train a local-only teacher baseline
* evaluate heldout region labels separately from weak sources
* export distilled high-confidence parser hints
