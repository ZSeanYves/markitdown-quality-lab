# Benchmark Chapter 1

MoonBit markitdown benchmark markdown sample 1 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 1
- bullet two with [link 1](https://example.com/markdown/1)
- bullet three with inline `code_1` and table signal

> Quote block 1: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 1
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 1 | group-1 | UTF-8 文本 |

Footnote ref [^1] appears here.

[^1]: Stable footnote body for chapter 1.

# Benchmark Chapter 2

MoonBit markitdown benchmark markdown sample 2 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 2
- bullet two with [link 2](https://example.com/markdown/2)
- bullet three with inline `code_2` and table signal

> Quote block 2: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 2
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 2 | group-2 | UTF-8 文本 |

Footnote ref [^2] appears here.

[^2]: Stable footnote body for chapter 2.

# Benchmark Chapter 3

MoonBit markitdown benchmark markdown sample 3 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 3
- bullet two with [link 3](https://example.com/markdown/3)
- bullet three with inline `code_3` and table signal

> Quote block 3: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 3
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 3 | group-3 | UTF-8 文本 |

Footnote ref [^3] appears here.

[^3]: Stable footnote body for chapter 3.

# Benchmark Chapter 4

MoonBit markitdown benchmark markdown sample 4 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 4
- bullet two with [link 4](https://example.com/markdown/4)
- bullet three with inline `code_4` and table signal

> Quote block 4: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 4
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 4 | group-4 | UTF-8 文本 |

Footnote ref [^4] appears here.

[^4]: Stable footnote body for chapter 4.

# Benchmark Chapter 5

MoonBit markitdown benchmark markdown sample 5 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 5
- bullet two with [link 5](https://example.com/markdown/5)
- bullet three with inline `code_5` and table signal

> Quote block 5: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 5
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 5 | group-5 | UTF-8 文本 |

Footnote ref [^5] appears here.

[^5]: Stable footnote body for chapter 5.

# Benchmark Chapter 6

MoonBit markitdown benchmark markdown sample 6 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 6
- bullet two with [link 6](https://example.com/markdown/6)
- bullet three with inline `code_6` and table signal

> Quote block 6: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 6
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 6 | group-6 | UTF-8 文本 |

Footnote ref [^6] appears here.

[^6]: Stable footnote body for chapter 6.

# Benchmark Chapter 7

MoonBit markitdown benchmark markdown sample 7 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 7
- bullet two with [link 7](https://example.com/markdown/7)
- bullet three with inline `code_7` and table signal

> Quote block 7: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 7
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 7 | group-0 | UTF-8 文本 |

Footnote ref [^7] appears here.

[^7]: Stable footnote body for chapter 7.

# Benchmark Chapter 8

MoonBit markitdown benchmark markdown sample 8 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 8
- bullet two with [link 8](https://example.com/markdown/8)
- bullet three with inline `code_8` and table signal

> Quote block 8: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 8
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 8 | group-1 | UTF-8 文本 |

Footnote ref [^8] appears here.

[^8]: Stable footnote body for chapter 8.

# Benchmark Chapter 9

MoonBit markitdown benchmark markdown sample 9 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 9
- bullet two with [link 9](https://example.com/markdown/9)
- bullet three with inline `code_9` and table signal

> Quote block 9: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 9
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 9 | group-2 | UTF-8 文本 |

Footnote ref [^9] appears here.

[^9]: Stable footnote body for chapter 9.

# Benchmark Chapter 10

MoonBit markitdown benchmark markdown sample 10 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 10
- bullet two with [link 10](https://example.com/markdown/10)
- bullet three with inline `code_10` and table signal

> Quote block 10: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 10
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 10 | group-3 | UTF-8 文本 |

Footnote ref [^10] appears here.

[^10]: Stable footnote body for chapter 10.

# Benchmark Chapter 11

MoonBit markitdown benchmark markdown sample 11 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 11
- bullet two with [link 11](https://example.com/markdown/11)
- bullet three with inline `code_11` and table signal

> Quote block 11: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 11
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 11 | group-4 | UTF-8 文本 |

Footnote ref [^11] appears here.

[^11]: Stable footnote body for chapter 11.

# Benchmark Chapter 12

MoonBit markitdown benchmark markdown sample 12 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 12
- bullet two with [link 12](https://example.com/markdown/12)
- bullet three with inline `code_12` and table signal

> Quote block 12: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 12
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 12 | group-5 | UTF-8 文本 |

Footnote ref [^12] appears here.

[^12]: Stable footnote body for chapter 12.

# Benchmark Chapter 13

MoonBit markitdown benchmark markdown sample 13 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 13
- bullet two with [link 13](https://example.com/markdown/13)
- bullet three with inline `code_13` and table signal

> Quote block 13: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 13
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 13 | group-6 | UTF-8 文本 |

Footnote ref [^13] appears here.

[^13]: Stable footnote body for chapter 13.

# Benchmark Chapter 14

MoonBit markitdown benchmark markdown sample 14 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 14
- bullet two with [link 14](https://example.com/markdown/14)
- bullet three with inline `code_14` and table signal

> Quote block 14: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 14
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 14 | group-0 | UTF-8 文本 |

Footnote ref [^14] appears here.

[^14]: Stable footnote body for chapter 14.

# Benchmark Chapter 15

MoonBit markitdown benchmark markdown sample 15 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 15
- bullet two with [link 15](https://example.com/markdown/15)
- bullet three with inline `code_15` and table signal

> Quote block 15: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 15
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 15 | group-1 | UTF-8 文本 |

Footnote ref [^15] appears here.

[^15]: Stable footnote body for chapter 15.

# Benchmark Chapter 16

MoonBit markitdown benchmark markdown sample 16 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 16
- bullet two with [link 16](https://example.com/markdown/16)
- bullet three with inline `code_16` and table signal

> Quote block 16: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 16
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 16 | group-2 | UTF-8 文本 |

Footnote ref [^16] appears here.

[^16]: Stable footnote body for chapter 16.

# Benchmark Chapter 17

MoonBit markitdown benchmark markdown sample 17 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 17
- bullet two with [link 17](https://example.com/markdown/17)
- bullet three with inline `code_17` and table signal

> Quote block 17: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 17
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 17 | group-3 | UTF-8 文本 |

Footnote ref [^17] appears here.

[^17]: Stable footnote body for chapter 17.

# Benchmark Chapter 18

MoonBit markitdown benchmark markdown sample 18 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 18
- bullet two with [link 18](https://example.com/markdown/18)
- bullet three with inline `code_18` and table signal

> Quote block 18: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 18
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 18 | group-4 | UTF-8 文本 |

Footnote ref [^18] appears here.

[^18]: Stable footnote body for chapter 18.

# Benchmark Chapter 19

MoonBit markitdown benchmark markdown sample 19 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 19
- bullet two with [link 19](https://example.com/markdown/19)
- bullet three with inline `code_19` and table signal

> Quote block 19: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 19
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 19 | group-5 | UTF-8 文本 |

Footnote ref [^19] appears here.

[^19]: Stable footnote body for chapter 19.

# Benchmark Chapter 20

MoonBit markitdown benchmark markdown sample 20 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 20
- bullet two with [link 20](https://example.com/markdown/20)
- bullet three with inline `code_20` and table signal

> Quote block 20: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 20
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 20 | group-6 | UTF-8 文本 |

Footnote ref [^20] appears here.

[^20]: Stable footnote body for chapter 20.

# Benchmark Chapter 21

MoonBit markitdown benchmark markdown sample 21 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 21
- bullet two with [link 21](https://example.com/markdown/21)
- bullet three with inline `code_21` and table signal

> Quote block 21: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 21
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 21 | group-0 | UTF-8 文本 |

Footnote ref [^21] appears here.

[^21]: Stable footnote body for chapter 21.

# Benchmark Chapter 22

MoonBit markitdown benchmark markdown sample 22 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 22
- bullet two with [link 22](https://example.com/markdown/22)
- bullet three with inline `code_22` and table signal

> Quote block 22: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 22
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 22 | group-1 | UTF-8 文本 |

Footnote ref [^22] appears here.

[^22]: Stable footnote body for chapter 22.

# Benchmark Chapter 23

MoonBit markitdown benchmark markdown sample 23 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 23
- bullet two with [link 23](https://example.com/markdown/23)
- bullet three with inline `code_23` and table signal

> Quote block 23: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 23
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 23 | group-2 | UTF-8 文本 |

Footnote ref [^23] appears here.

[^23]: Stable footnote body for chapter 23.

# Benchmark Chapter 24

MoonBit markitdown benchmark markdown sample 24 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 24
- bullet two with [link 24](https://example.com/markdown/24)
- bullet three with inline `code_24` and table signal

> Quote block 24: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 24
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 24 | group-3 | UTF-8 文本 |

Footnote ref [^24] appears here.

[^24]: Stable footnote body for chapter 24.

# Benchmark Chapter 25

MoonBit markitdown benchmark markdown sample 25 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 25
- bullet two with [link 25](https://example.com/markdown/25)
- bullet three with inline `code_25` and table signal

> Quote block 25: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 25
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 25 | group-4 | UTF-8 文本 |

Footnote ref [^25] appears here.

[^25]: Stable footnote body for chapter 25.

# Benchmark Chapter 26

MoonBit markitdown benchmark markdown sample 26 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 26
- bullet two with [link 26](https://example.com/markdown/26)
- bullet three with inline `code_26` and table signal

> Quote block 26: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 26
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 26 | group-5 | UTF-8 文本 |

Footnote ref [^26] appears here.

[^26]: Stable footnote body for chapter 26.

# Benchmark Chapter 27

MoonBit markitdown benchmark markdown sample 27 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 27
- bullet two with [link 27](https://example.com/markdown/27)
- bullet three with inline `code_27` and table signal

> Quote block 27: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 27
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 27 | group-6 | UTF-8 文本 |

Footnote ref [^27] appears here.

[^27]: Stable footnote body for chapter 27.

# Benchmark Chapter 28

MoonBit markitdown benchmark markdown sample 28 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 28
- bullet two with [link 28](https://example.com/markdown/28)
- bullet three with inline `code_28` and table signal

> Quote block 28: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 28
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 28 | group-0 | UTF-8 文本 |

Footnote ref [^28] appears here.

[^28]: Stable footnote body for chapter 28.

# Benchmark Chapter 29

MoonBit markitdown benchmark markdown sample 29 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 29
- bullet two with [link 29](https://example.com/markdown/29)
- bullet three with inline `code_29` and table signal

> Quote block 29: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 29
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 29 | group-1 | UTF-8 文本 |

Footnote ref [^29] appears here.

[^29]: Stable footnote body for chapter 29.

# Benchmark Chapter 30

MoonBit markitdown benchmark markdown sample 30 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 30
- bullet two with [link 30](https://example.com/markdown/30)
- bullet three with inline `code_30` and table signal

> Quote block 30: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 30
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 30 | group-2 | UTF-8 文本 |

Footnote ref [^30] appears here.

[^30]: Stable footnote body for chapter 30.

# Benchmark Chapter 31

MoonBit markitdown benchmark markdown sample 31 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 31
- bullet two with [link 31](https://example.com/markdown/31)
- bullet three with inline `code_31` and table signal

> Quote block 31: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 31
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 31 | group-3 | UTF-8 文本 |

Footnote ref [^31] appears here.

[^31]: Stable footnote body for chapter 31.

# Benchmark Chapter 32

MoonBit markitdown benchmark markdown sample 32 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 32
- bullet two with [link 32](https://example.com/markdown/32)
- bullet three with inline `code_32` and table signal

> Quote block 32: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 32
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 32 | group-4 | UTF-8 文本 |

Footnote ref [^32] appears here.

[^32]: Stable footnote body for chapter 32.

# Benchmark Chapter 33

MoonBit markitdown benchmark markdown sample 33 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 33
- bullet two with [link 33](https://example.com/markdown/33)
- bullet three with inline `code_33` and table signal

> Quote block 33: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 33
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 33 | group-5 | UTF-8 文本 |

Footnote ref [^33] appears here.

[^33]: Stable footnote body for chapter 33.

# Benchmark Chapter 34

MoonBit markitdown benchmark markdown sample 34 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 34
- bullet two with [link 34](https://example.com/markdown/34)
- bullet three with inline `code_34` and table signal

> Quote block 34: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 34
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 34 | group-6 | UTF-8 文本 |

Footnote ref [^34] appears here.

[^34]: Stable footnote body for chapter 34.

# Benchmark Chapter 35

MoonBit markitdown benchmark markdown sample 35 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 35
- bullet two with [link 35](https://example.com/markdown/35)
- bullet three with inline `code_35` and table signal

> Quote block 35: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 35
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 35 | group-0 | UTF-8 文本 |

Footnote ref [^35] appears here.

[^35]: Stable footnote body for chapter 35.

# Benchmark Chapter 36

MoonBit markitdown benchmark markdown sample 36 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 36
- bullet two with [link 36](https://example.com/markdown/36)
- bullet three with inline `code_36` and table signal

> Quote block 36: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 36
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 36 | group-1 | UTF-8 文本 |

Footnote ref [^36] appears here.

[^36]: Stable footnote body for chapter 36.

# Benchmark Chapter 37

MoonBit markitdown benchmark markdown sample 37 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 37
- bullet two with [link 37](https://example.com/markdown/37)
- bullet three with inline `code_37` and table signal

> Quote block 37: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 37
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 37 | group-2 | UTF-8 文本 |

Footnote ref [^37] appears here.

[^37]: Stable footnote body for chapter 37.

# Benchmark Chapter 38

MoonBit markitdown benchmark markdown sample 38 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 38
- bullet two with [link 38](https://example.com/markdown/38)
- bullet three with inline `code_38` and table signal

> Quote block 38: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 38
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 38 | group-3 | UTF-8 文本 |

Footnote ref [^38] appears here.

[^38]: Stable footnote body for chapter 38.

# Benchmark Chapter 39

MoonBit markitdown benchmark markdown sample 39 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 39
- bullet two with [link 39](https://example.com/markdown/39)
- bullet three with inline `code_39` and table signal

> Quote block 39: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 39
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 39 | group-4 | UTF-8 文本 |

Footnote ref [^39] appears here.

[^39]: Stable footnote body for chapter 39.

# Benchmark Chapter 40

MoonBit markitdown benchmark markdown sample 40 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 40
- bullet two with [link 40](https://example.com/markdown/40)
- bullet three with inline `code_40` and table signal

> Quote block 40: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 40
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 40 | group-5 | UTF-8 文本 |

Footnote ref [^40] appears here.

[^40]: Stable footnote body for chapter 40.

# Benchmark Chapter 41

MoonBit markitdown benchmark markdown sample 41 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 41
- bullet two with [link 41](https://example.com/markdown/41)
- bullet three with inline `code_41` and table signal

> Quote block 41: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 41
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 41 | group-6 | UTF-8 文本 |

Footnote ref [^41] appears here.

[^41]: Stable footnote body for chapter 41.

# Benchmark Chapter 42

MoonBit markitdown benchmark markdown sample 42 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 42
- bullet two with [link 42](https://example.com/markdown/42)
- bullet three with inline `code_42` and table signal

> Quote block 42: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 42
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 42 | group-0 | UTF-8 文本 |

Footnote ref [^42] appears here.

[^42]: Stable footnote body for chapter 42.

# Benchmark Chapter 43

MoonBit markitdown benchmark markdown sample 43 mixes prose, UTF-8 文本, links, lists, quotes, tables, and fenced code.

- bullet one for chapter 43
- bullet two with [link 43](https://example.com/markdown/43)
- bullet three with inline `code_43` and table signal

> Quote block 43: deterministic content for parser throughput and footnote normalization coverage.

```text
code fence 43
alpha beta gamma
```

| col_a | col_b | col_c |
| --- | --- | --- |
| 43 | group-1 | UTF-8 文本 |

Footnote ref [^43] appears here.

[^43]: Stable footnote body for chapter 43.

